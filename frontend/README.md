# Flatmate — Frontend

Run the frontend (requires Node.js):

1. cd frontend
2. npm install
3. npm run dev

This scaffolds a simple React + Vite frontend. Auth, posts, and chat use localStorage for now. Backend will be implemented next.
